<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= contact.first_name %> <%= contact.last_name %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css">
    <!-- <link rel="stylesheet" href="/style.css"> -->
</head>
<body>
    <header class="d-flex justify-content-between container-fluid navbar navbar-dark bg-dark">
        <div class="navbar-brand"> <a href="/contacts" class="text-decoration-none text-light">User Management</a></div>
    </header>

    <main class="container pt-5 pb-5">
        <ol class="breadcrumb">
            <li class="breadcrumb-item" aria-current="page"><a href="/contacts">Contacts</a></li>
            <li class="breadcrumb-item active" aria-current="page"><%= contact.first_name %> <%= contact.last_name %></li>
          </ol>
        <div class="d-flex align-items-center">
            <h2 class="mx-3"><%= contact.first_name %> <%= contact.last_name %></h2> <h4 class="text-secondary">#<%= contact.id %></h4>
        </div>
        <h3 class="text-secondary"><%= contact.email %></h3>
        <p class="lead">
            <%= contact.comment %>
        </p>
        <hr>
        <h4>Created at: <%= new Date(contact.created_at).getDate() %>/<%= new Date(contact.created_at).getMonth() %>/<%= new Date(contact.created_at).getFullYear() %></h4>
        <h4><strong>STATUS:</strong> <%= contact.status %> </h4>

        <div class="fluid d-flex justify-content-center">
            <a href="/contacts/edit-contact/<%= contact.id %>">
                <button class="btn btn-dark mx-3"><i class="bi bi-pencil-square"></i>Edit Contact</button>
            </a> 
            
            <button class="btn btn-danger mx-3"><i class="bi bi-trash"></i>Delete Contact</button>
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
</body>
</html>