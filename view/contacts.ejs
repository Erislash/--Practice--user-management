<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contacts</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css">
</head>
<body class="bg-light">
    <header class="d-flex justify-content-between container-fluid navbar navbar-dark bg-dark">
        <div class="navbar-brand"> <a href="/contacts" class="text-decoration-none text-light">User Management</a></div>
        <form class="d-flex" method="POST" action="/contacts">
            <fieldset class="d-flex">
                <input name="search" id="search" class="form-control me-2" type="search" placeholder="Search..." aria-label="Search">
                <button type="submit" class="btn btn-outline-light">Search</button>
            </fieldset>
        </form>
    </header>
    <main class="container pt-5 pb-5">
        <a href="/contacts/new-contact">
    
            <button class="btn btn-dark btn-outline-light">
                <i class="bi bi-plus"></i> Add Contact
            </button>
        </a>

        <hr>
        <% if(contacts.length > 0) {%>
        <div class="accordion accordion-flush">
            <% contacts.forEach(({id, first_name, last_name, email, phone, comment, status}) => { %>
                <div class="accordion-item">
                    <h2 class="accordion-header" id="contact<%= id %>">
                      <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapse<%= id %>" aria-expanded="false" aria-controls="collapse<%= id %>"><strong><%= first_name %> <%= last_name %></strong></button>
                    </h2>
                    <div id="collapse<%= id %>" class="accordion-collapse collapse" aria-labelledby="contact<%= id %>">
                      <ul class="accordion-body list-group p-0">
                        <li class="list-group-item"><strong>E-Mail:</strong> <%= email %></li>
                        <li class="list-group-item"><strong>Phone:</strong> <%= phone %></li>
                        <li class="list-group-item"><strong>Status:</strong> <%= status %></li>
                        <li class="list-group-item"><a class="btn btn-dark w-100" href="/contacts/contact/<%=id%>">More Details</a></li>
                      </ul>
                    </div>
                  </div>
            <% }); %>    
        </div>
        <%}else{%>
            <h2 class="warning">No contacts found</h2>
        <%}%>
    </main>






    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/js/bootstrap.bundle.min.js" integrity="sha384-JEW9xMcG8R+pH31jmWH6WWP0WintQrMb4s7ZOdauHnUtxwoG2vI5DkLtS3qm9Ekf" crossorigin="anonymous"></script>
</body>
</html>